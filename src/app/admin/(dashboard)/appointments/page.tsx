import { updateAppointmentStatusAction } from "@/app/actions";
import { getAppointments } from "@/lib/data";
import { formatDateTime } from "@/lib/utils";
export default async function AppointmentsAdmin(){const items=await getAppointments();return <><div className="admin-head"><div><h1>Appointments</h1><p>Manage vehicle viewings and test-drive requests.</p></div></div><div className="panel table-wrap"><table><thead><tr><th>Customer</th><th>Vehicle</th><th>Date</th><th>Type</th><th>Status</th><th>Update</th></tr></thead><tbody>{items.map(item=><tr key={item.id}><td><strong>{item.full_name}</strong><br/><span className="meta">{item.phone}</span></td><td>{item.vehicle?.make} {item.vehicle?.model}</td><td>{formatDateTime(item.scheduled_at)}</td><td>{item.appointment_type.replace("_"," ")}</td><td><span className={`status status-${item.status}`}>{item.status}</span></td><td><form action={updateAppointmentStatusAction} style={{display:"flex",gap:8}}><input type="hidden" name="id" value={item.id}/><select name="status" defaultValue={item.status}><option value="pending">Pending</option><option value="confirmed">Confirmed</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option><option value="no_show">No-show</option></select><button className="button button-outline" type="submit">Save</button></form></td></tr>)}</tbody></table>{!items.length&&<div className="empty">No appointments yet.</div>}</div></>}
