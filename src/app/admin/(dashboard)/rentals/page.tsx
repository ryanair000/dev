import { updateRentalStatusAction } from "@/app/actions";
import { getRentalRequests } from "@/lib/data";
import { formatCurrency, formatDateTime } from "@/lib/utils";
export default async function RentalsAdmin(){const items=await getRentalRequests();return <><div className="admin-head"><div><h1>Rental requests</h1><p>Review dates, availability and customer details.</p></div></div><div className="panel table-wrap"><table><thead><tr><th>Customer</th><th>Vehicle</th><th>Pickup</th><th>Return</th><th>Estimate</th><th>Status</th><th>Decision</th></tr></thead><tbody>{items.map(item=><tr key={item.id}><td><strong>{item.full_name}</strong><br/><span className="meta">{item.phone} • {item.reference}</span></td><td>{item.vehicle?.make} {item.vehicle?.model}</td><td>{formatDateTime(item.pickup_at)}</td><td>{formatDateTime(item.return_at)}</td><td>{formatCurrency(item.estimated_amount)}</td><td><span className={`status status-${item.status}`}>{item.status}</span></td><td><form action={updateRentalStatusAction} style={{display:"grid",gap:7,minWidth:210}}><input type="hidden" name="id" value={item.id}/><select name="status" defaultValue={item.status}><option value="pending">Pending</option><option value="approved">Approved</option><option value="rejected">Rejected</option><option value="active">Active</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option></select><input name="internal_notes" defaultValue={item.internal_notes??""} placeholder="Private note"/><button className="button button-blue" type="submit">Save</button></form></td></tr>)}</tbody></table>{!items.length&&<div className="empty">No rental requests yet.</div>}</div></>}
