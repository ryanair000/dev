import Link from "next/link";
import { loginAction } from "@/app/actions";
type SearchParams=Promise<{error?:string}>;
export default async function LoginPage({searchParams}:{searchParams:SearchParams}){const params=await searchParams;return <main className="login-page"><section className="login-visual"><Link href="/" className="brand brand-dark"><span className="brand-mark">S</span><span><strong>STEPONE</strong><small>AUTODEALERS</small></span></Link><h1>Everything your dealership needs in one clean dashboard.</h1><p>Manage vehicles, enquiries, rental requests, appointments and website content.</p></section><section className="login-form-wrap"><form action={loginAction} className="panel login-card"><span className="pill pill-red">Secure admin access</span><h2>Sign in to StepOne</h2>{params.error&&<div className="flash flash-error">{decodeURIComponent(params.error)}</div>}<div className="field"><label>Email address</label><input name="email" type="email" autoComplete="email" required/></div><div className="field" style={{marginTop:16}}><label>Password</label><input name="password" type="password" autoComplete="current-password" required/></div><button className="button button-primary" style={{width:"100%",marginTop:22}} type="submit">Sign in securely</button></form></section></main>}
